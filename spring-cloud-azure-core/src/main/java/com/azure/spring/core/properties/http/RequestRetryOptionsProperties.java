package com.azure.spring.core.properties.http;

public class RequestRetryOptionsProperties {

    // public static String PREFIX = "spring.cloud.azure.proxy";

    // TODO: each builder uses different retry options.
}
